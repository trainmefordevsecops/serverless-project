# Fargate with Terraform
This repo creates vpc, subnet,routetable ,ig gateway, nat gateway etc. along with ecs cluster using far gate

## changes to be made ##
replace bucket name in provider.tf, aws account no. in variables.tf


